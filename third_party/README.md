# Third-party integration

独立仓库创建后，在这里添加固定版本的 `simai-flow-scheduler` Git submodule：

```text
third_party/simai-flow-scheduler/
```

当前目录仍嵌套在 SimAI 仓库中，不能把父仓库反向添加成自己的 submodule，因此这里只保留边界说明，不创建循环 Git 关系。
